# ═════════════════════════════════════════════════════════════════════════════
# 🤖 LITELLM CONFIGURATION
# Auto-generated from blueprint-config.json
# ═════════════════════════════════════════════════════════════════════════════

# ═════════════════════════════════════════════════════════════════════════════
# 🤖 MODEL DEFINITIONS (Resolved from model-store)
# ═════════════════════════════════════════════════════════════════════════════
model_list:
  # ───────────────────────────────────────────────────────────────────────────
  # CLOUD MODELS (Resolved from model-store/cloud/)
  # ───────────────────────────────────────────────────────────────────────────
{% for model in litellm.models %}
  # {{ model.name }}{% if model.description %} - {{ model.description }}{% endif %}
  - model_name: {{ model.name }}
    litellm_params:
      model: {{ model.litellm_model_name }}
      api_key: os.environ/{{ model.requires_api_key }}
{% if model.max_output %}      max_tokens: {{ model.max_output }}
{% endif %}      stream: true
{% endfor %}
  
  # ───────────────────────────────────────────────────────────────────────────
  # LOCAL MODELS (Resolved from model-store/local/)
  # ───────────────────────────────────────────────────────────────────────────
  {% for key, model in local_inference.models %}
  {%- if model.enabled and model.group != "embeddings" %}

  # {{ model.display_name }} [{{ model.group }} group]
  - model_name: {{ model.name }}
    litellm_params:
      model: openai/{{ model.name }}
      api_base: http://{{ infrastructure.services.llama_swap.hostname }}:{{ infrastructure.services.llama_swap.port }}/v1
      api_key: "sk-no-key-required"
      stream: true
      custom_llm_provider: openai
  {% endif -%}
  {% endfor %}

  # ───────────────────────────────────────────────────────────────────────────
  # EMBEDDING MODELS (Resolved from model-store/local/)
  # ───────────────────────────────────────────────────────────────────────────
  {% for key, model in local_inference.models %}
  {%- if model.enabled and model.group == "embeddings" %}

  # {{ model.display_name }} [{{ model.group }} group]
  - model_name: {{ model.name }}
    litellm_params:
      model: openai/{{ model.name }}
      api_base: http://{{ infrastructure.services.llama_swap.hostname }}:{{ infrastructure.services.llama_swap.port }}/v1
      api_key: "sk-no-key-required"
      custom_llm_provider: openai
  {% endif -%}
  {% endfor %}

# ═════════════════════════════════════════════════════════════════════════════
# ⚙️  LITELLM SETTINGS
# ═════════════════════════════════════════════════════════════════════════════
litellm_settings:
  drop_params: {{ litellm.drop_params | default(true) | lower }}
  
  # Redis caching
  cache: true
  cache_params:
    type: redis
    host: {{ infrastructure.services.litellm_redis.hostname }}
    port: {{ infrastructure.services.litellm_redis.port }}
    ttl: 3600
  
  set_verbose: false

# ═════════════════════════════════════════════════════════════════════════════
# 🔧 GENERAL SETTINGS
# ═════════════════════════════════════════════════════════════════════════════
general_settings:
  database_url: "{{ litellm.database_url }}"
  master_key: os.environ/LITELLM_MASTER_KEY
  store_model_in_db: true
